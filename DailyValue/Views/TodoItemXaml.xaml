<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="DailyValue.TodoItemXaml">
    <ContentPage.Content>

    	<StackLayout VerticalOptions="StartAndExpand">

	        <Label Text="Name" />
	        <Entry Text="{Binding Path=AbbreviatedDescription}" x:Name="nameEntry" Placeholder="task name" />

			<Label Text="Notes" />
	        <Entry Text="{Binding Path=MainFoodDescription}" x:Name="notesEntry" />

	        <Button Text="Save" Clicked="OnSaveActivated" />
			
			<Button Text="Delete" Clicked="OnDeleteActivated" />

	        <Button Text="Cancel" Clicked="OnCancelActivated" />

	        <Button Text="Speak" Clicked="OnSpeakActivated" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>	